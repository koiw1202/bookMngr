<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
 
<mapper namespace="com.bookMngr.common.mybatis.mapper.UserMapper">

    <select id="selectUser" resultType="SelectUserOutDao">
        SELECT  member_cd
               ,login_cnt
               ,member_grade
               ,member_grant
               ,member_id
               ,nick_nm
               ,password
               ,phone_number
               ,reger_dt
               ,rst_yn
               ,unreg_yn
         FROM MEMBER
         WHERE member_id = 'abcd123'
    </select>

    <insert id="joinUserInfoByMyBatis">
        <selectKey>

        </selectKey>

        INSERT INTO bookmngr.`member` (
          member_cd
        , member_grade
        , member_grant
        , password) VALUES (
          #{memberCd}
        , #{memberGrade}
        , 'U'
        , #{password}
        )
    </insert>

</mapper>

















